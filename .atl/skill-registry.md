# Skill Registry — api (ODEPA Price Assistant)

Auto-generated by sdd-init on 2026-04-13.

## Project Standards

### Stack

- TypeScript 5.8 (strict, ESM, NodeNext modules)
- Hono 4.x web framework
- Supabase (PostgreSQL + Auth)
- Vercel AI SDK v6 (multi-provider LLM)
- Zod 4.x validation
- Jest 29 + ts-jest (ESM)

### Conventions

- Route pattern: one Hono router per file, exported as `{name}Router`
- Validation: Zod schemas with `@hono/zod-validator`
- Auth: `requireAuth` (mandatory) or `optionalAuth` (guest)
- Import style: `.js` extensions (ESM + NodeNext)
- Error format: `{ error: string }` with HTTP status code
- Linting: ESLint + Prettier (single quotes, 100 char width)
- Commits: conventional commits (`feat:`, `fix:`, etc.)

## User Skills (Triggers)

| Skill                       | Triggers                                                        | Relevance                         |
| --------------------------- | --------------------------------------------------------------- | --------------------------------- |
| agent-tools                 | inference.sh, ai model, flux, veo, claude api, image generation | Low — no AI inference calls       |
| api-design-principles       | REST API design, API specs, API standards                       | **HIGH** — Hono REST API project  |
| audit-website               | SEO audit, website health, broken links                         | Low — not a website               |
| azure-prepare               | deploy to azure, create app                                     | Low — Railway deployment          |
| azure-validate              | validate azure, preflight checks                                | Low — Railway deployment          |
| backend-testing             | REST API testing, unit tests, integration tests                 | **HIGH** — Jest + Supertest setup |
| brainstorming               | creative work, new features, design before implementation       | Medium — feature planning         |
| branch-pr                   | create pull request, open PR                                    | Medium — PR workflow              |
| changelog-generator         | changelog, release notes                                        | Medium — uses standard-version    |
| documentation-lookup        | React, Next.js, Prisma, Supabase, Hono docs                     | **HIGH** — needs Hono/AI SDK docs |
| find-skills                 | how do I do X, find a skill                                     | Low — meta skill                  |
| frontend-design             | build web components, landing pages                             | Low — no frontend in this repo    |
| go-testing                  | Go tests, Bubbletea TUI                                         | N/A — TypeScript project          |
| interface-design            | dashboards, admin panels, interactive tools                     | Low — no UI in this repo          |
| issue-creation              | create issue, report bug, request feature                       | Medium — issue workflow           |
| judgment-day                | judgment day, adversarial review                                | Medium — code review              |
| refactor                    | extract functions, rename variables, design patterns            | **HIGH** — evolving codebase      |
| skill-creator               | create skill, add agent instructions                            | Low — meta skill                  |
| vitest                      | Vitest testing framework                                        | N/A — uses Jest                   |
| vercel-react-best-practices | React/Next.js performance                                       | N/A — no React                    |

## Compact Rules (for sub-agent injection)

### api-design-principles (HIGH relevance)

- RESTful resource naming: plural nouns, consistent hierarchy
- Use query params for filtering, path params for identification
- Return appropriate HTTP status codes (200, 201, 400, 401, 404, 409, 500)
- Validate input with Zod at the route level via `zValidator`
- Structure responses consistently: data objects or error objects
- Version API paths if breaking changes are needed

### backend-testing (HIGH relevance)

- Jest 29 + ts-jest in ESM mode (`NODE_OPTIONS=--experimental-vm-modules`)
- Test files in `__tests__/` with `.test.ts` suffix
- Use `supertest` or Hono's `app.request()` for route testing
- Coverage command: `npm test` (includes `--coverage`)
- Pure helper functions should be exported for direct unit testing
- Mock Supabase client for unit tests, use test DB for integration

### documentation-lookup (HIGH relevance)

- Key dependencies: Hono, Vercel AI SDK (`ai` package), Supabase JS client, Zod
- Check docs when unsure about Hono middleware patterns or AI SDK streaming APIs

### refactor (HIGH relevance)

- Prefer extracting pure helper functions from route handlers
- Keep route handlers thin — delegate to service/helper functions
- Use TypeScript strict types (no `any` unless absolutely necessary)
- Follow existing patterns: one router per domain file
